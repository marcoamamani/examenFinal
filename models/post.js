//Escriba la estructura de la tabla dentro de las llaves
module.exports= function(sequelize, DataTypes){
	return sequelize.define('Post', {});
	<laber for="apellido paterno"
	class="rem"> apellido paterno: </label>
}